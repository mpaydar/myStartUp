/** “Quiet spot” filter: fewer than this many reviews (exclusive). */
export const QUIET_SPOT_MAX_REVIEWS_EXCLUSIVE = 50;
/** “Quiet spot” filter: average rating strictly below this (exclusive). */
export const QUIET_SPOT_MAX_RATING_EXCLUSIVE = 4;
/** When quiet filter is on, how many Google Text Search pages to merge (≤20 results per page). */
export const QUIET_SPOTS_SEARCH_MAX_PAGES = 5;
